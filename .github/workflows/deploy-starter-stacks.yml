name: Deploy StarterStacks SliDev Presentation

on:
  push:
    branches: [ feature/starter-stacks-slidev ]
    paths: [ 'starter-stacks-presentation/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'starter-stacks-presentation/**' ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'starter-stacks-presentation/package-lock.json'
        
    - name: Install dependencies
      run: |
        cd starter-stacks-presentation
        npm ci
        
    - name: Install Playwright
      run: |
        cd starter-stacks-presentation
        npx playwright install-deps chromium
        
    - name: Build SliDev presentation
      run: |
        cd starter-stacks-presentation
        npm run build
        
    - name: Setup Pages
      if: github.ref == 'refs/heads/feature/starter-stacks-slidev'
      uses: actions/configure-pages@v4
      
    - name: Upload artifact
      if: github.ref == 'refs/heads/feature/starter-stacks-slidev'
      uses: actions/upload-pages-artifact@v3
      with:
        path: 'starter-stacks-presentation/dist'
        
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/feature/starter-stacks-slidev'
      id: deployment
      uses: actions/deploy-pages@v4

  # Additional job for PDF export (optional)
  export-pdf:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/feature/starter-stacks-slidev'
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'starter-stacks-presentation/package-lock.json'
        
    - name: Install dependencies
      run: |
        cd starter-stacks-presentation
        npm ci
        
    - name: Install Playwright
      run: |
        cd starter-stacks-presentation
        npx playwright install chromium
        
    - name: Export to PDF
      run: |
        cd starter-stacks-presentation
        npm run export
        
    - name: Upload PDF artifact
      uses: actions/upload-artifact@v4
      with:
        name: starter-stacks-presentation-pdf
        path: 'starter-stacks-presentation/slides-export.pdf'
        retention-days: 30
